<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday My Love 💕</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Poppins:wght@300;500&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      font-family: 'Poppins', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      color: #fff;
      text-align: center;
    }

    .card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(12px);
      border-radius: 24px;
      padding: 30px 25px;
      width: 90%;
      max-width: 420px;
      box-shadow: 0 25px 50px rgba(0,0,0,0.25);
      animation: fadeIn 2s ease;
    }

    h1 {
      font-family: 'Great Vibes', cursive;
      font-size: 3rem;
      margin-bottom: 10px;
      animation: heartbeat 1.5s infinite;
    }

    h2 {
      font-weight: 300;
      margin-bottom: 20px;
    }

    p {
      font-size: 1rem;
      line-height: 1.7;
      margin-bottom: 15px;
    }

    .heart {
      position: absolute;
      bottom: -50px;
      font-size: 1.8rem;
      animation: floatUp 7s linear infinite;
      opacity: 0.8;
    }

    .heart:nth-child(1) { left: 15%; animation-delay: 0s; }
    .heart:nth-child(2) { left: 35%; animation-delay: 2s; }
    .heart:nth-child(3) { left: 55%; animation-delay: 4s; }
    .heart:nth-child(4) { left: 75%; animation-delay: 1s; }

    @keyframes floatUp {
      0% { transform: translateY(0); }
      100% { transform: translateY(-120vh); }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    @keyframes heartbeat {
      0% { transform: scale(1); }
      50% { transform: scale(1.08); }
      100% { transform: scale(1); }
    }

    footer {
      margin-top: 20px;
      font-size: 0.85rem;
      opacity: 0.9;
    }
  </style>
</head>
<body>

  <!-- Floating hearts -->
  <div class="heart">❤️</div>
  <div class="heart">💖</div>
  <div class="heart">💗</div>
  <div class="heart">❤️</div>

  <div class="card">
    <h1>Happy Birthday</h1>
    <h2>My Love <span id="name">❤️</span></h2>

    <p>
      On this special day, I just want you to know  
      how incredibly beautiful you are — inside and out ✨
    </p>

    <p>
      Thank you for being my happiness,  
      my safe place, and my favorite person 💕
    </p>

    <p>
      I promise to keep loving you today,  
      tomorrow, and always 💍
    </p>

    <footer>
      Forever yours 💞
    </footer>
  </div>

  <script>
    // Personalize with URL: ?name=Alisha
    const params = new URLSearchParams(window.location.search);
    const name = params.get("name");
    if (name) {
      document.getElementById("name").textContent = name;
    }
  </script>

</body>
</html>
